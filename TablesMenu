#! /usr/bin/bash
shopt -s extglob


function table_menu() {
    select choice in "Create Table" "Back"
    do
        case $choice in
            "Create Table") create_table ;;

            "Back") break ;;
        esac
    done
}

